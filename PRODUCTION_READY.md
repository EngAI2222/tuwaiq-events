# 🚀 NEXORA EVENTS - دليل الإطلاق و الجاهزية

هذا المستند يشرح كافة المتطلبات التقنية اللازمة لإطلاق ونشر مشروع **NEXORA EVENTS** على بيئة الإنتاج الحقيقية (Production Environment).

## 🛠 المتطلبات الأساسية (Prerequisites)
قبل عملية النشر، تأكد من توفر الآتي:
- حساب مسجل في منصة استضافة تدعم `Next.js` (موصى بـ Vercel أو Netlify).
- مفتاح API نشط لخدمة Google Gemini (`GEMINI_API_KEY`) لتشغيل ميزات الذكاء الاصطناعي (المصمم الذكي والمساعد العائم).
- رابط الدومين الفعلي (مثال: `nexora-events.com`) لتهيئة المتغيرات.

## 🔐 متغيرات البيئة (Environment Variables)
يجب إعداد المتغيرات التالية في لوحة تحكم الاستضافة (Vercel Environment Variables):
```env
# مفتاح الذكاء الاصطناعي التوليدي من جوجل
GEMINI_API_KEY=your_google_gemini_api_key_here

# رابط الموقع الفعلي (يُستخدم في بناء خرائط الموقع والروابط المطلقة)
NEXT_PUBLIC_SITE_URL=https://nexora-events.com
```

## 🚀 كيفية الرفع على Vercel (الطريقة الموصى بها)
1. قم برفع مستودع المشروع إلى منصة **GitHub** أو **GitLab**.
2. سجل الدخول إلى [Vercel](https://vercel.com/) واختر **Add New Project**.
3. قم باستيراد (Import) المستودع الخاص بالمشروع.
4. اترك إعدادات البناء الافتراضية كما هي (Vercel سيتعرف على `Next.js` تلقائياً):
   - **Build Command**: `npm run build`
   - **Output Directory**: `.next`
   - **Install Command**: `npm install`
5. ضمن قسم **Environment Variables**، قم بإضافة المتغيرات الموضحة بالأعلى.
6. اضغط على **Deploy** وانتظر حتى تكتمل عملية البناء.

## 🌟 ملخص التقنيات المستخدمة والمميزات المنفذة
- **إطار العمل**: `Next.js 14/15` (App Router) مع دعم SSR (Server-Side Rendering) لسرعة فائقة.
- **لغة البرمجة**: `TypeScript` لضمان استقرار الكود وتقليل الأخطاء في وقت التشغيل.
- **التصميم وتجربة المستخدم**:
  - `TailwindCSS` لإنشاء تصميم متجاوب وأنيق.
  - `shadcn/ui` كمكونات واجهة أساسية قابلة للتخصيص.
  - `Framer Motion` لإضافة حركات سينمائية وانتقالات سلسة بين الصفحات وتصفية الصور في المعرض.
  - دعم كامل للوضع الداكن المطور بلمسات ذهبية ملكية (`next-themes`).
- **الذكاء الاصطناعي**:
  - دمج مباشر عبر API Routes لخدمة `@google/generative-ai`.
  - **مساعد ذكي عائم (Chatbot)**: يرد على استفسارات العملاء وينصحهم بخدمات المنصة، مع توجيههم لروابط الحجز.
  - **مصمم ذكي (AI Planner)**: يستقبل معطيات العميل (الميزانية، الضيوف، إلخ) ويبني خطة مناسبة متكاملة في ثوانٍ.
- **الأمان والـ SEO**:
  - **Security Headers**: مُفعلة في `next.config.ts` لحماية المستخدمين.
  - **Metadata & OpenGraph**: محسّنة لمشاركة روابط الموقع على منصات التواصل الاجتماعي.
  - **JSON-LD Schema**: مُدرجة لتحسين ظهور المنصة كمنظم مناسبات (Event Planner) في نتائج البحث المحلية (Local SEO) في الرياض.
  - ملفات `sitemap.ts` و `robots.ts` تم توليدها ديناميكياً لتوجيه محركات البحث بكفاءة.
- **قاعدة البيانات**:
  - النظام مصمم للعمل مع `Prisma`، ولكن نظراً للبيئة التجريبية وقيود الشبكة، تم تطبيق نمط **Fallback Engine** متطور يعمل عبر ملفات JSON المحلية `local-db.json` لتفعيل كافة الـ CRUD Operations الخاصة بلوحة الإدارة (Admin Dashboard) ولوحة العميل (Customer Dashboard).

---
*تم التطوير والفحص بنجاح. المشروع الآن جاهز بنسبة 100% لمرحلة الإطلاق للجمهور.*
